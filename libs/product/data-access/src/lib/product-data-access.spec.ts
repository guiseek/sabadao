import { productDataAccess } from './product-data-access';

describe('productDataAccess', () => {
  it('should work', () => {
    expect(productDataAccess()).toEqual('product-data-access');
  });
});
